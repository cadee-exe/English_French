def spaces(y, t):
    

    spaces("CCC..", ".C.C.C")

spots = int(input("C"))
yesterday_spots = input("CCC..")
today_spots = input(".C.C.C")
 
counter = 0

for i in range(0, spots):
    if yesterday_spots[i] == "C" and today_spots[i] == "C" :
        counter+=1
print(counter)